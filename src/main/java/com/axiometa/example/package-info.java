/**
 * Runnable usage examples. Nothing in this package is framework API; it shows
 * how the pieces from the other packages are wired together into a complete
 * optimization run.
 *
 * <p>{@link com.axiometa.example.OneMaxExample} solves the classic OneMax
 * problem with the generational genetic algorithm and prints a run summary.
 * {@link com.axiometa.example.OneMaxProblem} is the example's problem
 * definition; it deliberately mirrors the S7 test fixture (which stays in
 * test sources per D47) so the example is self-contained in main sources.
 */
package com.axiometa.example;
